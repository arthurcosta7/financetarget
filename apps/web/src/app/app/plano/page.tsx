import type { Metadata } from "next";

import { SubscriptionCenter } from "@/components/SubscriptionCenter";

export const metadata: Metadata = { title: "Plano e comunicações — FinanceTarget" };

export default function PlanoPage() { return <SubscriptionCenter />; }
